# Hysteria Backend - Configuration
# Copy to .env: cp docker.env.example .env
# ALL SECRETS ARE REQUIRED! The app will not start without them.

# ==================== REQUIRED ====================

# Panel domain (without https://)
PANEL_DOMAIN=panel.example.com

# Email for the Let's Encrypt certificate
ACME_EMAIL=admin@example.com

# SSH password encryption key (EXACTLY 32 characters! Generate: openssl rand -hex 16)
ENCRYPTION_KEY=

# Session secret (generate: openssl rand -hex 32)
SESSION_SECRET=

# MongoDB password (generate: openssl rand -hex 16)
MONGO_PASSWORD=
MONGO_USER=hysteria

# ==================== OPTIONAL ====================

# Redis URL (default: redis://redis:6379)
# REDIS_URL=redis://redis:6379

# IP whitelist for the panel and API (comma-separated, empty = allow all)
# Examples: 127.0.0.1, 192.168.1.0/24
PANEL_IP_WHITELIST=

# Sync interval with nodes (in minutes)
SYNC_INTERVAL=2

# Log level (error, warn, info, debug)
# debug - shows all logs including debug output
# info - only important events (default)
LOG_LEVEL=info
